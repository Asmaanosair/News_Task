# News Task Project

A Laravel project to fetch and manage articles from multiple news sources (NewsAPI, NewsCred, OpenNews, etc.) with filtering and search functionality.

---

## Setup & Run

Follow these steps to run the project using Docker:

### 1. Build and start Docker containers

```bash
docker-compose up -d --build
